# A developer has built a three-tier application and needs to verify security g...

A developer has built a three-tier application and needs to verify security group rules that have been setup by a security engineer: Presentation Tier - Accessed by users over the web, protected by the security group, presentation-sg Logic Tier - RESTful API accessed from the Presentation Tier via https, protected by the security group, logic-sg Data Tier - SQL Server database accessed over port 1433 from the Logic Tier, protected by the security group, data-sg What combination of the following rules will allow the security group to secure the application? (Select THREE.)

- A) presentation-sg: Allow ports 80 and 443 from 0.0.0.0/0 - This rule allows access to the Presentation Tier over the web via HTTP (port 80) and HTTPS (port 443) from anywhere.
- B) data-sg: Allow port 1433 from presentation-sg - This rule would allow access from the Presentation Tier to the Data Tier's SQL Server database, which is not the desired configuration according to the application's architecture.
- C) data-sg: Allow port 1433 from logic-sg - This rule allows access to the Data Tier SQL Server database from the Logic Tier via port 1433, as required by the application architecture.
- D) presentation-sg: Allow port 1433 from data-sg - This rule would allow access from the Data Tier to the Presentation Tier, which is not necessary for the application's functionality.
- E) logic-sg: Allow port 443 from presentation-sg - This rule allows access to the Logic Tier RESTful API from the Presentation Tier via HTTPS (port 443).
- F) logic-sg: Allow port 443 from 0.0.0.0/0 - This rule would allow access to the Logic Tier RESTful API over HTTPS from anywhere, which may not align with the desired security posture.

## Answer

**A, C, E**

- **A) presentation-sg: Allow ports 80 and 443 from 0.0.0.0/0 - This rule allows access to the Presentation Tier over the web via HTTP (port 80) and HTTPS (port 443) from anywhere.**
  - presentation-sg: Allow ports 80 and 443 from 0.
  - 0.0.0/0 - This rule allows access to the Presentation Tier over the web via HTTP (port 80) and HTTPS (port 443) from anywhere.

- **C) data-sg: Allow port 1433 from logic-sg - This rule allows access to the Data Tier SQL Server database from the Logic Tier via port 1433, as required by the application architecture.**
  - data-sg: Allow port 1433 from logic-sg - This rule allows access to the Data Tier SQL Server database from the Logic Tier via port 1433, as required by the application architecture.

- **E) logic-sg: Allow port 443 from presentation-sg - This rule allows access to the Logic Tier RESTful API from the Presentation Tier via HTTPS (port 443).**
  - logic-sg: Allow port 443 from presentation-sg - This rule allows access to the Logic Tier RESTful API from the Presentation Tier via HTTPS (port 443).

## Why the other options are incorrect

- **B) data-sg: Allow port 1433 from presentation-sg - This rule would allow access from the Presentation Tier to the Data Tier's SQL Server database, which is not the desired configuration according to the application's architecture.**
  - data-sg: Allow port 1433 from presentation-sg - This rule would allow access from the Presentation Tier to the Data Tier's SQL Server database, which is not the desired configuration according to the application's architecture.

- **D) presentation-sg: Allow port 1433 from data-sg - This rule would allow access from the Data Tier to the Presentation Tier, which is not necessary for the application's functionality.**
  - presentation-sg: Allow port 1433 from data-sg - This rule would allow access from the Data Tier to the Presentation Tier, which is not necessary for the application's functionality.

- **F) logic-sg: Allow port 443 from 0.0.0.0/0 - This rule would allow access to the Logic Tier RESTful API over HTTPS from anywhere, which may not align with the desired security posture.**
  - logic-sg: Allow port 443 from 0.
  - 0.0.0/0 - This rule would allow access to the Logic Tier RESTful API over HTTPS from anywhere, which may not align with the desired security posture.
