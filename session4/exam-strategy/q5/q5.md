# Each time a developer publishes a new version of an AWS Lambda function, all ...

Each time a developer publishes a new version of an AWS Lambda function, all the dependent event source mappings need to be updated with the reference to the new version’s Amazon Resource Name (ARN). These updates are time consuming and error-prone. Which combination of actions should the developer take to avoid performing these updates when publishing a new Lambda version? (Select TWO.)

- A) A Lambda event source mapping can either point to the version ARN, or an alias ARN, but not to the l
- B) A Lambda alias is a pointer to a specific Lambda function version.
- C) This solution does not address your problem. Every alias has its own unique ARN. Therefore, you woul
- D) A Lambda alias cannot point to another Lambda alias, only to a Lambda function version.
- E) Instead of using ARNs for the Lambda function in event source mappings, you can use an alias ARN. Yo

## Answer

**B, E**

- **B) **
  - A Lambda alias is a pointer to a specific Lambda function version.

- **E) **
  - Instead of using ARNs for the Lambda function in event source mappings, you can use an alias ARN.
  - You do not need to update your event source mappings when you promote a new version or roll back to a previous version.

## Why the other options are incorrect

- **A) **
  - A Lambda event source mapping can either point to the version ARN, or an alias ARN, but not to the layer ARN.

- **C) **
  - This solution does not address your problem.
  - Every alias has its own unique ARN.
  - Therefore, you would still need to update the event source mapping with the new ARN when a new version is published.

- **D) **
  - A Lambda alias cannot point to another Lambda alias, only to a Lambda function version.
